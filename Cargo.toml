[package]
name = "iec_61850_lib"
version = "0.1.0"
authors = ["Jakob Vogelsang <jakob-vogelsang@posteo.de>"]
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
rasn = "0.18"
hex = "0.4.3"

[dev-dependencies]
# Add test dependencies here if needed
